<img width="763" height="348" alt="image" src="https://github.com/user-attachments/assets/b32565b6-36b9-4b70-a8a7-a980f43c9d14" />

สรุปการตั้งค่า DHCP และ DNS Server ใน Cisco Packet Tracer
ภาพรวมของระบบ 
มี Server 1 เครื่อง (Server0) 
และคอมพิวเตอร์ 3 เครื่อง (PC0, PC1, PC2) 
เชื่อมต่อกันผ่าน Switch สิ่งที่เราต้องการคือ ไม่อยากไปนั่งตั้งค่า IP Address ให้คอมพิวเตอร์ทีละเครื่อง แต่จะให้ Server ทำหน้าที่แจก IP ให้อัตโนมัติ และทำหน้าที่จำชื่อเว็บไซต์จำลองในระบบ

<img width="871" height="301" alt="image" src="https://github.com/user-attachments/assets/6a7b6c87-c9dc-4665-a145-24f91bfd7d8e" />
ขั้นตอนที่ 1: การตั้งค่า IP Address ให้ Server 
ก่อนที่ Server จะไปให้บริการเครื่องอื่นได้ ตัวมันเองต้องมีที่อยู่ (IP) ที่แน่นอนก่อน

ทำไมต้องเลือก Static: Server ห้ามเปลี่ยน IP ไปมาเด็ดขาด (เหมือนเบอร์โทรศัพท์ของคอลเซ็นเตอร์ที่ต้องเป็นเบอร์เดิมเสมอ) เพื่อให้เครื่องลูกข่าย (Client) สามารถติดต่อเข้ามาขอรับบริการได้ตลอดเวลา

การตั้งค่า:

IPv4 Address (192.168.10.10): ตั้งค่า IP คงที่ให้ Server

Subnet Mask (255.255.255.0): ระบุขนาดและวงของเครือข่าย

Default Gateway (192.168.10.1): ชี้ทางออกไปยัง Router (ถ้ามีการเชื่อมต่ออินเทอร์เน็ตหรือข้ามวงแลน)

(ข้อสังเกต) DNS Server (192.68.10.10): ตรงนี้อาจจะพิมพ์ตกตัวเลข 1 ไปครับ ความจริงควรจะเป็น 192.168.10.10 (ชี้กลับมาที่ตัวเอง เพราะ Server ตัวนี้ทำหน้าที่เป็น DNS ด้วย)

<img width="873" height="407" alt="image" src="https://github.com/user-attachments/assets/f37d2a9f-73e0-4eb0-a3d8-9656fed2c6d3" />
ขั้นตอนที่ 2: การตั้งค่าบริการ DNS 
DNS (Domain Name System) คือสมุดโทรศัพท์ของเครือข่าย ทำหน้าที่แปลงชื่อที่คนจำง่าย (เช่น netlab.local) ให้เป็นตัวเลข IP Address ที่คอมพิวเตอร์เข้าใจ

ทำไมต้องทำ: เวลาคนใช้งานอยากเข้าเว็บเซิร์ฟเวอร์ จะได้ไม่ต้องพิมพ์ตัวเลข 192.168.10.10 แต่พิมพ์ชื่อ netlab.local แทน

การตั้งค่า:

Name: netlab.local (ชื่อโดเมนที่ต้องการ)

Address: 192.168.10.10 (IP ของ Server)

(ข้อควรระวัง!) ในภาพของคุณ DNS Service ถูกติ๊กอยู่ที่คำว่า "Off" อย่าลืมเปลี่ยนเป็น "On" ด้วยนะครับ ไม่อย่างนั้นระบบแปลงชื่อจะไม่ทำงาน

<img width="870" height="533" alt="image" src="https://github.com/user-attachments/assets/fe1bcb3c-db79-4d6a-ab4d-7068d19ee05c" />
ขั้นตอนที่ 3: การตั้งค่าบริการ DHCP
DHCP (Dynamic Host Configuration Protocol) คือบริการแจก IP Address อัตโนมัติให้กับอุปกรณ์ใดๆ ก็ตามที่เสียบสายแลนเข้ามาในเครือข่าย

ทำไมต้องทำ: เพื่อความสะดวกสบาย สมมติว่ามีคอมพิวเตอร์ 100 เครื่อง ถ้าไม่มี DHCP เราต้องเดินไปตั้งค่า IP เองทีละเครื่อง แต่ถ้ามี DHCP แค่เสียบสายก็ใช้งานได้เลย

การตั้งค่า (Pool Name: serverPool):

Service: ต้องติ๊กเป็น "On" เพื่อเปิดใช้งาน

Default Gateway: 192.168.10.1 (แจกทางออกให้เครื่องลูกข่าย)

DNS Server: 192.68.10.10 (แจกที่อยู่ DNS ให้เครื่องลูกข่าย - แนะนำให้แก้เป็น 192.168.10.10 ตามข้อสังเกตด้านบน)

Start IP Address: 192.168.10.100 (กำหนดว่าให้เริ่มแจก IP ตั้งแต่เบอร์ 100 เป็นต้นไป เพื่อไม่ให้ไปซ้ำกับเบอร์ 1-99 ที่เราอาจจะกันไว้ตั้งให้ Server หรืออุปกรณ์สำคัญตัวอื่นๆ)

Maximum Number of Users: 156 (แจกได้สูงสุด 156 เครื่อง)

<img width="869" height="334" alt="image" src="https://github.com/user-attachments/assets/94dac977-690d-45d1-8a28-78fd65973e3b" />
ขั้นตอนที่ 4: การตั้งค่าเครื่อง PC เพื่อรับ IP (จากภาพ image_b6bd1c.png)
เมื่อ Server พร้อมแล้ว เราก็มาบอกให้เครื่องลูกข่ายไปขอ IP จาก Server

ทำไมต้องเลือก DHCP: เพื่อบอกคอมพิวเตอร์ว่า "ไม่ต้องใช้ IP แบบกำหนดเองนะ ให้ตะโกนถามหา Server ในเครือข่าย แล้วเอา IP ที่ Server แจกมาใช้งาน"

ผลลัพธ์ที่ได้: จะเห็นว่าพอกดปุ่ม DHCP เครื่อง PC0 จะได้รับ IP เป็น 192.168.10.100 (ซึ่งเป็นเบอร์แรกสุดตามที่เราตั้ง Start IP ไว้ในขั้นตอนที่ 3) พร้อมทั้งได้รับค่า Subnet, Gateway และ DNS มาอย่างครบถ้วนโดยที่เราไม่ต้องพิมพ์เองเลย

สรุปสิ่งที่ได้เรียนรู้วันนี้:
คุณได้เรียนรู้วิธีการลดภาระของผู้ดูแลระบบ (Admin) ด้วยการใช้ DHCP ในการแจก IP และอำนวยความสะดวกให้ผู้ใช้งานด้วยการตั้งชื่อให้จำง่ายผ่าน DNS
